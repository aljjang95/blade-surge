# RSI LOOP — 회전 정의

> 한 회전 = 하나의 콘텐츠 축을 골라 만들고, 자동 채점으로 통과시키고, 커밋·푸시·배포하고, 배운 것을 적는다.
> **채점을 통과하지 못한 회전은 커밋하지 않는다.** 되돌리고 다음 회전으로 간다.

## 회전 1회 — 8단계

### 1. 고르기
`PRD.md §4` 우선순위 목록에서 **하나만** 고른다. 두 개 고르면 실패 원인을 못 가린다.
직전 회전과 같은 축은 연속 2회까지만. 3회째는 다른 축으로 넘어간다.

### 2. 기준선 뜨기
```bash
npm run build && node tools/metrics.mjs --out .rsi/base.json
```
지금 상태의 점수를 먼저 박아둔다. 이게 없으면 "좋아졌다"를 말할 수 없다.

### 3. 만들기
- **뼈대는 절차 생성** — 규칙과 시드로 뽑는다. 데이터 파일이 커지는 방향은 피한다
- **외형은 생성 에셋** — 이미지가 2장 이상 필요하면 **반드시 `parallel-gpt-image` 스킬**
  (1세션 / 1프롬프트 / 단일이미지 10장. 이미지 바이트를 채팅 컨텍스트로 흘리지 않는다)
- 새 콘텐츠는 **새 동사**를 가져야 한다. 스탯만 다른 리스킨은 PRD §5 위반

### 4. 자동 채점 (게이트 A — 필수)
```bash
npm run build && node tools/metrics.mjs --out .rsi/head.json --shots .rsi/shots
node tools/metrics.mjs --compare .rsi/base.json .rsi/head.json
```
- `PRD.md §2` 밴드를 **하나라도** 벗어나면 실패
- `errors > 0` 이면 다른 건 볼 것도 없이 실패
- 회귀 판정: 기준선 대비 `avgFrameMs` +15% 초과, 또는 `drawCalls` +20% 초과면 실패

### 5. 스크린샷 대조 (게이트 B — 필수)
`.rsi/shots/` 의 3~4장을 **직접 눈으로 본다.** 지표는 통과하는데 화면이 망가진 경우가 실제로 있었다
(엘리트 틴트가 텍스처를 노란 덩어리로 뭉갠 적 있음 — `LESSONS.md` 참조).
- HUD 겹침, 텍스처 소실, 조명 붕괴, 새 에셋의 스케일/축 오류를 본다
- 새로 만든 것이 **스크린샷에 실제로 보이는지** 확인한다. 안 보이면 만든 게 아니다

### 6. 교차 리뷰 (게이트 C — 축 2·4·6에서만)
스킬·직업·몬스터처럼 **재미와 밸런스가 걸린 축**은 `multi-model-e2e-review` 스킬로
다른 모델에게 교차 검증시킨다. 외형/아바타/맵테마 회전에서는 생략(비용 대비 효과 없음).
- 모든 지적은 소스 대조 후 반영한다. 오탐이 3~4할 나오는 게 정상이다

### 7. 밸런스 시뮬 (수치가 바뀐 회전에서만)
```bash
node tools/metrics.mjs --sim 2000
```
DPS·생존시간·성장곡선·가챠 기댓값이 목표 밴드 안인지. 스킬/세트/직업/강화를 건드렸으면 필수.

### 8. 닫기
```bash
git add -A && git commit -m "feat(<축>): <무엇을>"
git push
npm run deploy          # Cloudflare Workers
```
그리고 `LESSONS.md` 에 **한 줄**을 적는다 — 이번에 걸려 넘어진 것, 다음 회전이 피해야 할 것.
지표가 좋아졌다는 기록은 필요 없다(파일에 남는다). **실패한 것만** 적는다.

## 실패 처리

게이트에서 떨어지면:
1. `git checkout -- .` 로 되돌린다. 반쯤 고쳐서 통과시키려 하지 않는다
2. `LESSONS.md` 에 왜 떨어졌는지 적는다
3. 같은 축을 더 작게 쪼개서 재시도하거나, 다른 축으로 넘어간다

**연속 2회 실패하면 축을 바꾼다.** 3회째 같은 축을 붙잡는 건 루프가 아니라 고집이다.

## 회전 크기

한 회전은 **1~2시간 분량**. 그보다 크면 실패했을 때 되돌릴 게 너무 많고,
작으면 게이트 비용이 본작업보다 커진다.
"신규 직업 하나"는 한 회전이다. "직업 시스템 개편"은 회전이 아니라 프로젝트다 — 쪼개라.

## 루프를 멈춰야 할 때

- `PRD.md §4` 우선순위 1~3번이 모두 끝났고, 지표가 전 항목 밴드 중앙에 있을 때
- 또는 대표님이 방향을 바꿀 때. 그때는 PRD부터 고치고 루프를 다시 연다
