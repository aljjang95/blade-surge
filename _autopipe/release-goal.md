# 2026-09-05 남은 구현·검증·배포 Goal

- 대표님 현재 승인: commit/push/PR review/merge/deploy. 별도 유료 호출·실결제·권한 확대 승인은 없음.
- 출발점: PR #2 `0bc8f0cc8e569b9a67fcaa53889812fa2be0a9ef`, live/main `fd71749b4ea26aa7050d008173e80c97550b9be2`. 단일 integrator, worker 0.
- 정체성: 무한의 성에서 직접 움직이고 적을 모아 콤보로 정리한다. 드랍·세트·강화로 다음 층에 도전하며 네브와 대화하고 함께 싸운다. 전용 외형은 이 전투 읽기와 손맛을 해치지 않아야 한다.
- 사전 검토: ChatGPT GPT-5.6 Sol / 매우 높음, `https://chatgpt.com/c/6a9bcd07-1678-83ee-a8ff-82ae57942db8`. 공개 API 호출 상한, 비동기 응답의 전투 권한 분리, 배포 가드 fail-closed를 우선 반영한다.
- 실행 기준: 변경 없는 PR2 빌드에서 로비→출격→공격→네브 상태 보고, 입력 잠금, 1280×720/360×740 캡처와 콘솔 오류 0 재확인.

| 요구 | 구현·검증 기준 | 현재 |
|---|---|---|
| 생성형 동행 | 서버가 역할/문맥/JSON 스키마를 재구성, 실제 한국어 응답 확인 | 코드·로컬 fixture 검증, 실제 호출 승인 대기 |
| 호출·비용 경계 | 동일 origin, body 제한, IP·전역 persistent 상한, 중복 요청/재시도/timeout 검사 | 승인별 100회/48시간·A→B→A·IP/날짜 변경 회귀 통과 |
| 전투 권한 | 모델은 전술 제안만, 적용은 사용자 버튼과 현재 전투 epoch 검증, 보상·승리 권한 없음 | 테스트·실제 UI 전술 선택 검증 |
| 대화 복귀 | 닫기/재출격/전투 종료/수동 전술 변경 시 늦은 응답 폐기, 1개 요청만 진행 | 테스트·실제 UI 검증 |
| 전용 외형 | TLL 불변 GLB·출처·SHA, 재질/clip/socket/축/단위 검증, 실제 전투 캡처 | 계약·배율·무기 길이 검증, TLL 미술 품질과 게임 채택 미완료 |
| 실기기 | Android 실제 입력/재개/프레임/사운드/대화 검증 | USB·mDNS 0, 연결 요청함 |
| 릴리스 | 전체 diff Pro 리뷰, 로컬 회귀, exact head PR/merge, 현재 live 조상성·버전·rollback 확인 | High 지적 해소·마지막 Medium 2건 수정 후 Pro 재검토 |

Cloudflare 대시보드 2026-09-05 실측: Workers Paid $5/month, 당시 AI 0 neurons. 10,000 neurons/day 무료 배정과 계정 전체 초과 과금은 별개다. 대화 생성은 기본 비활성으로 구현하고 실제 활성화 전 비용 경계를 확정한다. 앱에 가짜 생성 응답을 넣어 통과 처리하지 않는다.

TLL 제작 원본은 별도 사용자 작업이 소유한다. 완료된 제작 후보의 소스/Blender revision을 보존하고, 남은 게임용 미술 수정은 이 작업의 별도 복사본에서 진행한다. 검증된 불변 결과만 게임에 복사한다. physical device와 최종 FELT는 브라우저 viewport로 대체하지 않는다.

## 재개 후 검증

- 대표님이 실행 훅을 제거한 뒤 코드 수정을 재개했다. 기존 commit/push/PR/merge/deploy 승인 범위는 유지한다. 실제 AI 비용 승인과 물리 Android 연결은 별도 요청에 아직 응답이 없다.
- Pro 1차: High 2/Medium 8. 2차: High 종결, Medium 4 잔존. 마지막 4건은 원자 영수증 교체, 승인별 영속 원장, 초과 충전 에너지/시각 정확한 원복, authored 배율과 무기 세계 길이 분리로 수정했다.
- 3차 Pro의 Medium 2건은 rename 이후 디렉터리 동기화 실패 시 메모리와 정본 정렬·잠금 유지, 에너지 환급 시 본 저장/백업/복구 기준 동시 갱신으로 수정했다. 검증 기록 후 종료된 배포도 같은 소유자 영수증으로 재조정할 수 있다.
- 4차 Pro에서 이전 2건은 종결했다. 준비 실패 뒤 `--reconcile`이 새 잠금을 만들던 경로는 기존 동일-owner 잠금만 읽어 재개하도록 수정했다. 원격 호출 전 준비 상태의 잔존 잠금은 배포 없이 종료한다.
- `bun run check`: 73 tests / 469 assertions + 타입/빌드 통과. 실제 D1 잠금 경합 2건 중 1건 허용, 소유자 해제 확인, 배포 호출은 0회였다.
- 실제 IAB: 전체 초기화 후 gold12000/inventory0/동행gather/bond1/대화1/복구 원본null. localStorage getter SecurityError 조건에서도 동행 생성·출격·RAF/WebGL 루프·콘솔 오류0. 실패 환급은 VIP 만료 뒤 energy350/energyT123456을 모두 그대로 보존했다.
- 최종 완주: seed20260905, 180초/160처치/42드랍/최대17체/보상 공백28초/피격0.26/7박자/290draws/오류0/실제 승리. 이전 exact-build-metrics 정본과 회귀 없음. Chromium 고정 dt 계측이며 물리 폰 FPS가 아니다.
- 로비 동행 버튼의 등급/메뉴 겹침과, 열린 동행 대화를 자동 구매 창이 덮는 문제를 추가 확인하여 작은 배치 수정과 대화 소유권 검사로 보완했다.
- TLL `repro-final`: SHA a28daafc73063c3265cd2b5b7c07b208786fd546649c06f2f9f02fe2c9fffb62, 101041 triangles/17 bones/가중치 오류0 기술 통과. 원화 대비 갑옷 윤곽·동작 중 팔 형태는 미술 검수 미달이며 게임 기본 모델로 채택하지 않았다. TLL 원본과 작업 파일을 수정하지 않았다.
