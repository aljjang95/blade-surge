{
  "name": "blade-surge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && node tools/write-version.mjs",
    "preview": "vite preview --host",
    "deploy": "node tools/deploy-guard.mjs && npm run build && wrangler deploy",
    "metrics": "node tools/metrics.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
