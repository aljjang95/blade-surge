# BLADE SURGE — PRD

> 이 문서가 기준이다. RSI 루프의 모든 회전은 여기 적힌 **목표 밴드** 안으로 들어와야 통과한다.
> 재미는 취향이 아니라 측정 대상이다. 측정할 수 없게 쓴 요구사항은 요구사항이 아니다.

## 0. 한 줄

가로모드 웹앱에서 도는 **3D 몹몰이 핵앤슬래시 가챠 RPG**.
무한의 성을 층층이 올라가며 넓은 절차적 필드를 탐험하고, 미니맵으로 보스를 찾아 잡고,
쏟아지는 드랍으로 세트를 맞추고 강화해서 다음 층을 뚫는다.

## 1. 도파민 3축

게임의 모든 기능은 이 셋 중 하나 이상에 기여해야 한다. 아무 데도 안 붙으면 만들지 않는다.

| 축 | 정의 | 대표 장치 |
|---|---|---|
| **보는맛** | 화면이 터진다 | 블룸·색수차·히트플래시·방사형블러, 참격/마법진/화염기둥 VFX, 희귀도 빔, 데미지 숫자 |
| **손맛** | 입력이 몸에 붙는다 | 5~6타 콤보(돌진·회전·강타·부채꼴·노바 동사 포함, 홀드로 이어짐, 경타 슈퍼아머), 히트스탑, 넉백, 질주 가속, 퍼펙트 회피 슬로모, 스티키 락온, 카메라 리드/트라우마 쉐이크 |
| **듣는맛** | 때린 게 들린다 | 타격 레이어(clang/impact), 콤보 피치 상승, **던파식 기합·스킬 외침·피격 신음(영웅별 독자 목소리, 영어)**, 보스·나레이터 연기 대사, 보스 뿔피리, BGM 덕킹, 햅틱 |

### 도파민 리듬 — 한 층에서 반드시 일어나야 하는 것
```
탐험(궁금) → 조우(긴장) → 몹몰이 진공(해방) → 드랍 우수수(보상)
   → 세트 한 칸 채움(진전) → 보스 발견(경계) → 처치(승리) → 층 클리어 정산(정리)
```
이 8박자 중 **한 층에서 6개 이상**이 실제로 발화해야 한다. 자동 지표로 검사한다.

## 2. 목표 밴드 (통과 판정 기준)

`node tools/metrics.mjs` 가 뽑는 값. 밴드를 벗어나면 그 회전은 **실패**다.
**측정 조건은 레벨 1 SSR 영웅 · 1층** (첫 플레이어가 겪는 그대로). 적 수치의 기준도 이것이다 (`stages.js` 머리말).

| 지표 | 밴드 | 왜 |
|---|---|---|
| `errors` (console error + pageerror) | **0** | 협상 불가 |
| `bootMs` | ≤ 12000 | 첫 화면까지. 모바일 회선 감안 |
| `floorClearSec` | 150 – 420 | 너무 짧으면 허무하고, 길면 지루하다 |
| `killsPerFloor` | ≥ 35 | 몹몰이 게임이 몹이 적으면 안 된다 |
| `maxAliveSeen` | ≥ 14 | 화면에 무리가 보여야 몰이가 성립한다 |
| `dropsPerFloor` | ≥ 8 | 보상 간격이 30초를 넘으면 안 된다 |
| `longestDryStreakSec` | ≤ 35 | 아무 보상 없이 흐른 최장 구간 |
| `hitTakenRatio` | 0.05 – 0.45 | 0이면 긴장이 없고 0.45↑면 불합리하다 |
| `avgFrameMs` (SwiftShader) | ≤ 42 | 헤드리스 소프트웨어 렌더 기준. 실기는 훨씬 빠르다 |
| `p95FrameMs` | ≤ 90 | 스파이크가 손맛을 죽인다 |
| `rhythmBeats` | ≥ 6 / 8 | 위 8박자 발화 수 |
| `drawCalls` | ≤ 420 | 인스턴싱/머지가 무너졌는지 감지 |

## 3. 시스템

### 3.1 필드 — 무한의 성
- 층 = 시드 기반 절차 생성. 같은 층 번호 = 같은 지형(재현성, 디버깅용)
- 방 9~13개가 프론티어 성장식으로 퍼진다(한 줄로 늘어지지 않게) + 순환 통로 1~2개
- 방 타입: `START` / `NORMAL` / `ELITE` / `TREASURE` / `BOSS`
- 시작방에서 BFS 최장 거리 = 보스방. 이동 마스크 + 벽 슬라이딩, BFS 거리장 길찾기
- **보스 봉인**: 보스방 복도 입구마다 결계. 다른 구역을 전부 정화해야 풀리고, 풀린 자리에 보스방 문 앞으로 가는 **포탈**이 열린다
- 구역 정화 = 장비 1 확정 + 골드. 보스방 입장 제물 장비 2, 보스 HP 20% 마다 파편(장비 1) — 보상 공백 30초 규칙을 지키는 장치
- **우상단 미니맵**: 안개, 방 타입 색, 적/드랍 도트, 보스 미발견 시 가장자리 방향 화살표

### 3.2 전투
- 동시 생존 상한은 그래픽 설정 연동(낮음 16 / 보통 24 / 높음 34)
- **진공(vacuum)**: 다수 스킬이 적을 끌어당긴 뒤 때린다. 기본 콤보 3타도 약하게 끈다
- 적 밀도에 따라 카메라 자동 줌아웃, AUTO는 가장 밀집한 무리를 조준
- 보스: 60%·30% 페이즈 전환, 30%부터 광폭화

### 3.3 성장 / 과금 (전부 목업 — 실제 결제 없음)
- 드랍 → 자석 흡수 → 세트 4종 → 강화 +0~+20 (+8까지 100%, +12부터 파괴 위험)
- 가챠 천장 80, 소프트천장 60, 10연 SR 보장
- 배틀패스 / VIP / 월정액 / 첫 결제 2배 / 부활 / 광고 2배

## 4. RSI 루프가 늘려야 할 것 (우선순위 순)

각 항목은 **뼈대는 절차 생성, 외형은 생성 에셋**으로 간다.

1. **장비에 따른 캐릭터 외형 변화** — 무기/방어구 장착이 실제 모델에 반영. 손맛보다 보는맛이지만 과금 동기가 가장 세다
   *(회전 5 완료: 무기 등급별 메시 교체 · 방어구 등급별 방패/보조 · 등급 발광 · +10 오라, 로비 쇼케이스 동일 — `src/game/look.js`. 남은 것: 테마 세트 4종 전용 외형)*
2. **레벨 구간별 스킬 생성** — 직업 × 레벨대로 스킬이 열린다. 새 스킬은 반드시 새로운 VFX + 새로운 진공/이동 패턴을 가져야 한다(색만 바꾼 스킬 금지)
   *(회전 7 완료: **각성** 슬롯 2칸(Lv.10 / Lv.20) × 4영웅 = 8종. HUD 5·6번 버튼(Q/E)이 잠금 상태로 항상 보이고 해금 레벨을 띄운다.
   각 각성기는 기존에 없던 동사를 하나씩 가진다 — 결박 · 경계장판 · 밀어모으기 · 순차분출+잔존균열 · 반사투사체 · 시간정지+각인 · 분신표식 · 순간이동연쇄.
   해금은 결과 화면 배너로 알린다. 남은 것: Lv.30 각성 III, 각성기 전용 보이스)*
3. **아이템 세트 확장** — 세트 효과가 플레이 방식을 바꿔야 한다(스탯 +N% 만 있는 세트 금지)
   *(회전 8 완료: 테마 세트 4종 → **8종**. 새 4종은 각각 새 동사를 가진다 —
   **서리결정**(적을 얼려 부수고 그 자리에 얼음 기둥을 세운다 = 플레이어가 지형을 만든다),
   **역병포자**(죽인 자리에 구름이 남고 그 안에서 죽이면 전염되어 자란다 = '어디서 죽이느냐'가 목표가 된다),
   **룬각인**(콤보로 장전 → 회피로 쏟아붓거나 아껴서 쿨타임을 지운다 = 자원 관리 축),
   **심연사슬**(락온한 적과 사슬로 이어지고, 회피하면 원심으로 휘감아 뭉친다 = 새 이동 패턴).
   HUD 에 세트 게이지 1줄(`#hud-setgauge`)이 붙어 장전·포자·기둥·사슬 상태가 보인다.
   구현은 `src/game/setprocs.js`, 검증은 `tools/shot_sets.mjs` (단언 17종). 남은 것: 테마 세트 전용 외형(§4-1))
4. **신규 직업** — 기존 4영웅과 조작 리듬이 달라야 한다(콤보 타수, 이동기, 자원 관리 중 최소 하나)
5. **캐시 아바타** — 성능에 영향 없는 순수 외형. 과금 목업의 최상단 SKU
6. **몬스터 종 확장** — 새 종은 새 AI 행동을 가져야 한다(스탯만 다른 리스킨 금지)
7. **맵 테마 확장** — 층 구간별 타일셋/조명/BGM

## 5. 하지 않을 것

- 실제 결제 연동 (목업 유지. 연결 지점만 `ui.js → paySheet(sku)`)
- CC0/자체생성이 아닌 에셋 사용
- 세로모드 최적화 (가로 우선, 세로는 회전 안내만)
- 서버·계정 시스템 (localStorage 세이브 유지)
- 스탯만 다른 리스킨 콘텐츠 — 위 4·6번 참조. 양보다 밀도

## 6. 용어

- **층(Floor)** — 절차 생성된 한 판. 보스 처치 시 클리어
- **구역(Room)** — 층 안의 방 하나. 첫 진입 시 스폰, 전멸 시 클리어
- **몹몰이** — 진공기로 다수를 끌어모아 한 번에 쓸어담는 핵심 행동
- **회전(Iteration)** — RSI 루프 1회. `RSI.md` 참조
