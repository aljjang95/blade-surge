# 추가 성능·성장 루프 패치

사용자 요구: 끊김을 더 줄이되 타격감과 이펙트를 유지한다. 성장·장비·던전 진행의 마찰을 실제 플레이로 찾아 개선하고 검증 후 배포한다.

- 기준: abfb4bd2d139fd4a5f0ed05c690970aabb0e9091 / PR7. 단일 integrator, worker0. 기존 스킬 컴파일 수정과 경제·전투 수치를 보존한다.
- 실행 표면: 실제 IAB WebGL2·1280×720 및360×740, 기존 Bun/게임 하네스, 실제 결과→장비 선택→다음 층. 실기기 FPS·실제 AI·전용 아트는 이 패치의 완료 주장이 아니다.
- 재현: 실제 한 층153처치·39드랍·3별 승리. 결과의 장비는 클릭해도 상세가 열리지 않고 로비→영웅→아이콘 찾기를 거친다. 가방44개는 이름 없이 +0만 표시한다. 상세에는 현재 장비 대비 최종 능력치·세트 변화와 닫기 버튼이 없다.
- 성능 재현:4초720프레임에서 입자 버퍼71,424,000바이트(99,200/프레임), 체력 텍스트720회 갱신. 끝의 활성 입자0/2/0. 먼저 활성 구간 전송과 값 변경 시 체력 텍스트 갱신만 적용하고 검증한다. 카메라 scratch 변경은 아직 채택하지 않는다.
- 성장 개선: 결과 장비를 눌러 해당 장비 비교로 진입, 장비 교체/해제의 실제 최종 스탯·세트 활성/해제 표시, 이름·부위 필터가 있는 가방, 정비 뒤 명확한 다음 층 목표·출격. 자동 장착·자동 강화·강제 정비 단계는 없다.
- 시각 계약: 기존 어두운 성채·금색 행동 버튼·아이템 등급 테두리와 자산을 유지한다. 선택한 전리품의 가치가 읽히는 것이 목표다. 숫자 비교→세트 효과→행동 순서, 모바일 단일 열·모달 스크롤·44px 조작, 색 외 +/- 및 문구로 변화를 표시한다. 새 효과·새 애니메이션은 추가하지 않는다.
- 수락: 입자0→burst→포화→0→재발사·가변dt/부분업로드 동일성, 같은 장면 전송량 및 프레임 분포, 실제 시전 프로그램 증가0. 장착 preview와 실제 heroStats 일치, 미리보기 저장 변경0, 세트2/4 및 다른 영웅 장비 이동, 장비 없는 상태·취소·좁은 화면·키보드. 기존 빠른 다음 진행·정산 중복 방지 유지. 전체 회귀·독립 Pro·정확한 HEAD와 라이브 자산 대조 후 완료.
- 사전 검토: Chrome GPT-5.6 Sol/매우 높음. 동일 장면과 분산을 고려하고 업로드 감소를 FPS 향상으로 과장하지 않으며, 장비 비교는 기존 계산 권위를 공유하고 선택권·빠른 다음 진행을 보존한다. 원문은 현재 작업 work/progression/preflight.txt.
- 상세 증거와 acceptance-matrix/v2는 `_autopipe/evidence/progression-*`에 생성한다. 배포는 기존 D1 lease·rollback 영수증 가드를 사용한다.

## 구현·회귀 기록

- 긴 2층 실제 전투에서16회 GPU 프로그램 생성/폐기와 렌더344.6ms를 잡았다. 로스터 생성→사망 격리 검사에서 해골 자객의 정적 양면 메시 사망마다2개 프로그램과약90ms가 재현됐다. 투명 사전 준비 대상을 SkinnedMesh에서 모든 Mesh로 넓혔다. 이후 반복 로스터 검사에서 프로그램 생성0.
- 적 제거마다 boneTexture와 발밑 마커가 남았다. Actor가 복제된 skeleton을 중복 없이 회수하고 Enemy가 소유 마커만 회수한다. 3회 로스터에서 geometry54/54/54,texture75/75/75, 원본 skeleton 공유0. 새 컨텍스트 첫 draw50.3ms 표본은 별도 보존하며 모든 기기 끊김0을 주장하지 않는다.
- 동일 정지 장면의 전체 전송 대조군/부분 전송/부분 전송/대조군:100프레임 각각9,920,000→2,302,144바이트. 최종 상세 검사의16개 픽셀 배열은 모두 일치했다. 한 앞선 실행의 마지막 표본 checksum 차이도 원본에 남겼다. p95범위는 겹치므로 전체 FPS 향상으로 주장하지 않는다.
- 실제 성장: 보상39개를 얻은 첫 층 뒤 역병의 낫 장착 예측3438→3894와 실제 전투력이 일치했다. 골드229로 레벨3, 전투력4132가 된 뒤 정비 화면에서2층 출격. 164처치/44드랍/185.97초/3별 승리 후 한설검 보상 클릭 한 번으로 비교(공격−4,치명+4%,전투력+16), 장비를 바꾸지 않고3층 선택까지 확인했다.
- Pro M1(부위 필터와 키보드 포커스 단절)은 결과 장비의 부위로 필터 전환, 모달 진입·Tab 순환·Escape·닫기/장착/판매 복귀로 수정했다. 실제 획득 영수증을 로컬 UI에 재생해 무기 필터→반지 결과 Enter→i-close→동일UID47 복귀를 검증했다. 정산 값은 변하지 않았다. 판매 취소는 상세로, 판매 완료는 해당 부위 필터로 복귀한다.
- Pro H1(동행 override가 부모 disposed 가드를 먼저 닫음)은 중복 override 제거로 수정했다. 실제 Director 전투→로비→재출격3회에서 매번 old root0, boneTexture2/2·material3/3회수, 새 동행 root1개. 부모 정리와 예약 callback 차단을 함께 보존한다.
- 최신 정적 검사:86 tests/580 assertions·타입·빌드 PASS. 마지막 독립 재검토와 배포 정본은 생성 receipt, matrix, 실제 version.json을 대조한다.
